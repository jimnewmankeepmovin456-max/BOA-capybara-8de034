<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BOANYC • Online Banking</title>
  <style>
    :root {
      --primary: #0a3d62;
      --accent: #00b894;
      --text: #2d3436;
      --bg: #ffffff;
      --card: #f8f9fa;
      --border: #e0e0e0;
    }
    [data-theme="dark"] {
      --primary: #0a3d62;
      --accent: #00d4a5;
      --text: #e0e0e0;
      --bg: #121212;
      --card: #1e1e1e;
      --border: #333;
    }
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: 'Segoe UI', system-ui, sans-serif;
      background: linear-gradient(135deg, #0a3d62, #00b894);
      min-height: 100vh;
      color: var(--text);
      transition: background 0.4s;
    }
    .container {
      max-width: 1280px;
      margin: 20px auto;
      background: var(--bg);
      border-radius: 24px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.3);
      overflow: hidden;
      min-height: calc(100vh - 40px);
    }
    header {
      background: var(--primary);
      color: white;
      padding: 18px 24px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      position: sticky;
      top: 0;
      z-index: 100;
    }
    .logo { font-size: 28px; font-weight: 700; }
    .header-right {
      display: flex;
      align-items: center;
      gap: 16px;
    }
    .theme-toggle {
      background: none;
      border: none;
      color: white;
      font-size: 22px;
      cursor: pointer;
      padding: 8px;
    }
    .login-screen, .dashboard {
      padding: 40px 24px;
    }
    .login-screen {
      max-width: 420px;
      margin: 80px auto;
      text-align: center;
    }
    h1 { color: var(--accent); margin-bottom: 12px; }
    .form-group {
      margin: 24px 0;
      text-align: left;
    }
    label {
      display: block;
      margin-bottom: 8px;
      font-weight: 600;
    }
    input {
      width: 100%;
      padding: 16px;
      border: 2px solid var(--border);
      border-radius: 12px;
      background: var(--card);
      color: var(--text);
      font-size: 16px;
    }
    button {
      background: var(--accent);
      color: white;
      border: none;
      padding: 16px;
      font-size: 17px;
      font-weight: 600;
      border-radius: 12px;
      cursor: pointer;
      width: 100%;
      margin-top: 12px;
    }
    button:hover { opacity: 0.9; transform: translateY(-2px); }
    .error { color: #ff6b6b; margin-top: 12px; }

    /* Dashboard */
    .dashboard { display: none; }
    .welcome {
      background: var(--card);
      padding: 28px;
      border-radius: 20px;
      margin-bottom: 32px;
      text-align: center;
    }
    .balance {
      font-size: 42px;
      font-weight: 700;
      color: var(--accent);
      margin: 12px 0;
    }
    .bottom-nav {
      display: none;
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      background: var(--card);
      border-top: 1px solid var(--border);
      padding: 8px 0;
      z-index: 200;
    }
    .bottom-nav a {
      display: flex;
      flex-direction: column;
      align-items: center;
      color: var(--text);
      text-decoration: none;
      font-size: 12px;
      flex: 1;
    }
    .bottom-nav a.active { color: var(--accent); }
    .nav-tabs {
      display: flex;
      overflow-x: auto;
      gap: 8px;
      padding-bottom: 12px;
      scrollbar-width: none;
    }
    .nav-tabs button {
      background: var(--card);
      border: 1px solid var(--border);
      padding: 10px 20px;
      border-radius: 30px;
      white-space: nowrap;
      color: var(--text);
    }
    .nav-tabs button.active {
      background: var(--accent);
      color: white;
      border: none;
    }
    .section { display: none; }
    .section.active { display: block; }
    .card {
      background: var(--card);
      padding: 24px;
      border-radius: 20px;
      margin-bottom: 20px;
      border: 1px solid var(--border);
    }
    .transaction {
      display: flex;
      justify-content: space-between;
      padding: 16px 0;
      border-bottom: 1px solid var(--border);
    }
    .transaction:last-child { border: none; }
    .recipient-card {
      padding: 18px;
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 16px;
      margin-bottom: 12px;
      cursor: pointer;
    }
    .recipient-card:hover {
      border-color: var(--accent);
    }
    .footer {
      text-align: center;
      padding: 24px;
      color: #777;
      font-size: 14px;
      background: var(--card);
    }

    /* Mobile improvements */
    @media (max-width: 768px) {
      .container { margin: 10px; border-radius: 16px; }
      .login-screen, .dashboard { padding: 24px 16px; }
      .bottom-nav { display: flex; }
      .nav-tabs { flex-wrap: nowrap; }
      .balance { font-size: 36px; }
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <div class="logo">BOANYC</div>
      <div class="header-right">
        <button class="theme-toggle" onclick="toggleTheme()" title="Toggle dark mode">🌙</button>
      </div>
    </header>

    <!-- Login -->
    <div id="loginScreen" class="login-screen">
      <h1>Welcome Back</h1>
      <p style="color:#888; margin-bottom:40px;">Sign in to manage your finances</p>
      
      <div class="form-group">
        <label>Username</label>
        <input type="text" id="username" placeholder="maggiestannard">
      </div>
      <div class="form-group">
        <label>Password</label>
        <input type="password" id="password">
      </div>
      
      <button onclick="handleLogin()">Sign In</button>
      <div id="loginError" class="error"></div>
    </div>

    <!-- Dashboard -->
    <div id="dashboard" class="dashboard">
      <div class="welcome">
        <h2>Welcome back, Maggie Stannard 👋</h2>
        <p style="margin:10px 0; color:#888;">March 30, 2026</p>
        <div style="color:#888;">Available Balance</div>
        <div class="balance">$100,000.00</div>
      </div>

      <!-- Bottom Navigation (mobile) -->
      <div class="bottom-nav">
        <a href="#" onclick="showSection(0);return false" class="active"><span>🏠</span><span>Home</span></a>
        <a href="#" onclick="showSection(1);return false"><span>↗️</span><span>Transfer</span></a>
        <a href="#" onclick="showSection(2);return false"><span>📋</span><span>Activity</span></a>
        <a href="#" onclick="showSection(3);return false"><span>💳</span><span>Cards</span></a>
        <a href="#" onclick="showSection(4);return false"><span>👤</span><span>Account</span></a>
      </div>

      <div class="nav-tabs" style="margin-bottom:24px;">
        <button onclick="showSection(0)" class="active" id="tab0">Home</button>
        <button onclick="showSection(1)" id="tab1">Transfer</button>
        <button onclick="showSection(2)" id="tab2">Activity</button>
        <button onclick="showSection(3)" id="tab3">Cards</button>
        <button onclick="showSection(4)" id="tab4">Account</button>
      </div>

      <!-- Home -->
      <div id="section0" class="section active">
        <div class="card">
          <h3>Quick Stats</h3>
          <p style="margin:12px 0;"><strong>Spent this month:</strong> $2,450.00</p>
          <p><strong>Income this month:</strong> $4,800.00</p>
        </div>
      </div>

      <!-- Transfer -->
      <div id="section1" class="section">
        <h3>Send Money</h3>
        <p style="color:#888; margin:12px 0 20px;">To approved recipients only</p>
        
        <div onclick="selectRecipient(this)" class="recipient-card">
          <strong>Sophie Williams</strong><br>
          20000027 • 30-00-27
        </div>
        <div onclick="selectRecipient(this)" class="recipient-card">
          <strong>Daniel Harris</strong><br>
          30000035 • 40-00-35
        </div>

        <div class="card">
          <label>Amount ($)</label>
          <input type="number" id="amount" placeholder="0.00" style="margin-bottom:16px;">
          <label>Reference</label>
          <input type="text" id="ref" placeholder="e.g. Rent">
          <button onclick="simulateTransfer()" style="margin-top:20px;">Send Transfer</button>
          <div id="transferMsg" style="margin-top:16px; font-weight:500;"></div>
        </div>
      </div>

      <!-- Transactions -->
      <div id="section2" class="section">
        <h3>Recent Activity</h3>
        <div class="card">
          <div class="transaction">
            <div>
              <strong>Transfer to Sophie Williams</strong><br>
              <small>Today • Reference: March rent</small>
            </div>
            <div style="color:#00b894; font-weight:600;">-$850.00</div>
          </div>
          <div class="transaction">
            <div>
              <strong>Salary Deposit</strong><br>
              <small>Mar 28 • From Employer</small>
            </div>
            <div style="color:#00b894; font-weight:600;">+$4,200.00</div>
          </div>
          <div class="transaction">
            <div>
              <strong>Grocery Store</strong><br>
              <small>Mar 27</small>
            </div>
            <div style="color:#e74c3c; font-weight:600;">-$124.50</div>
          </div>
        </div>
      </div>

      <!-- Cards -->
      <div id="section3" class="section">
        <h3>Your Cards</h3>
        <div class="card" style="background:linear-gradient(135deg,#1e3a8a,#3b82f6); color:white; padding:32px 24px; border-radius:20px;">
          <div style="font-size:22px; margin-bottom:40px;">•••• •••• •••• 4242</div>
          <div style="display:flex; justify-content:space-between;">
            <div>Maggie Stannard<br>Valid Thru 03/29</div>
            <div style="font-size:28px;">VISA</div>
          </div>
        </div>
        <p style="color:#888; text-align:center; margin-top:16px;">Virtual card • Tap to freeze</p>
      </div>

      <!-- Account -->
      <div id="section4" class="section">
        <div class="card">
          <h3>Account Details</h3>
          <p><strong>Holder:</strong> Maggie Stannard</p>
          <p><strong>Balance:</strong> $100,000.00</p>
          <p><strong>Type:</strong> Personal Checking</p>
        </div>
        <button onclick="logout()" style="background:#e74c3c; width:auto; padding:14px 32px; margin-top:20px;">Sign Out</button>
      </div>
    </div>

    <div class="footer">
      © 2026 BOANYC • Frontend UI demonstration for research & personal coding
    </div>
  </div>

  <script>
    const correctUser = "maggiestannard";
    const correctPass = "cYkn&#Rk6@SZnQ7";

    // Theme handling
    function setTheme(theme) {
      document.documentElement.setAttribute('data-theme', theme);
      localStorage.setItem('theme', theme);
    }

    function toggleTheme() {
      const current = document.documentElement.getAttribute('data-theme') || 'light';
      setTheme(current === 'dark' ? 'light' : 'dark');
    }

    // Load saved theme or system preference
    function initTheme() {
      const saved = localStorage.getItem('theme');
      if (saved) {
        setTheme(saved);
      } else if (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) {
        setTheme('dark');
      }
    }

    function handleLogin() {
      const user = document.getElementById("username").value.trim();
      const pass = document.getElementById("password").value;
      const err = document.getElementById("loginError");

      if (user === correctUser && pass === correctPass) {
        document.getElementById("loginScreen").style.display = "none";
        document.getElementById("dashboard").style.display = "block";
      } else {
        err.textContent = "Invalid credentials.";
      }
    }

    function showSection(n) {
      document.querySelectorAll('.section').forEach(s => s.classList.remove('active'));
      document.getElementById(`section${n}`).classList.add('active');

      document.querySelectorAll('.nav-tabs button').forEach((btn, i) => {
        btn.classList.toggle('active', i === n);
      });

      // Highlight bottom nav on mobile
      document.querySelectorAll('.bottom-nav a').forEach((a, i) => {
        a.classList.toggle('active', i === n);
      });
    }

    function selectRecipient(el) {
      document.querySelectorAll('.recipient-card').forEach(c => c.style.borderColor = 'var(--border)');
      el.style.borderColor = 'var(--accent)';
    }

    function simulateTransfer() {
      const amt = document.getElementById("amount").value;
      const msg = document.getElementById("transferMsg");
      if (!amt || parseFloat(amt) <= 0) {
        msg.style.color = "#ff6b6b";
        msg.textContent = "Enter a valid amount.";
        return;
      }
      msg.style.color = "#00b894";
      msg.textContent = "✅ Transfer initiated successfully.";
      setTimeout(() => { 
        document.getElementById("amount").value = "";
        document.getElementById("ref").value = "";
        msg.textContent = "";
      }, 3000);
    }

    function logout() {
      document.getElementById("dashboard").style.display = "none";
      document.getElementById("loginScreen").style.display = "block";
      document.getElementById("username").value = "";
      document.getElementById("password").value = "";
      document.getElementById("loginError").textContent = "";
    }

    // Keyboard support
    document.addEventListener("keypress", e => {
      if (e.key === "Enter" && document.getElementById("loginScreen").style.display !== "none") handleLogin();
    });

    // Initialize
    window.onload = () => {
      initTheme();
    };
  </script>
</body>
</html>